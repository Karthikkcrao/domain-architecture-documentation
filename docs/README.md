# Generated Documentation Directory

The agents create output files in this directory.

Expected domain-level outputs:

```text
domain-ecosystem-architecture.html
domain-repository-inventory.json
```

Expected repository-level outputs within each repository:

```text
domain-service-architecture.html
domain-service-inventory.json
```

Generated files are intentionally not included in this template package because they depend on the analyzed repositories.
